server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Single-page app: unknown paths (client-side routes like /interviews/:id)
    # fall back to index.html so deep links and refreshes work.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
